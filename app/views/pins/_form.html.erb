<%= form_with(model: pin, html: { multipart: true} ) do |form| %>
  <% if pin.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pin.errors.count, "error") %> prohibited this pin from being saved:</h2>

      <ul>
        <% pin.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="form-group">
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description, class: "form-control", style: "width:500px" %>
  </div>

  <div class="form-group">
    <%= form.label :pin_image, style: "display: block" %>
    <%= form.file_field :pin_image, class: "form-control" %>
  </div>

  <div>
    <%= form.submit "Submit", class: "btn btn-primary mt-2" %>
  </div>
<% end %>
